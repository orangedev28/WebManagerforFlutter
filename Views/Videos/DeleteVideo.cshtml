@model WebQuanLyAppOnTap.Models.Video

@{
    ViewBag.Title = "Xóa video bài giảng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@if (ViewData["Error"] != null)
{
    <div class="alert alert-danger" role="alert">
        @ViewData["Error"]
    </div>
}

<section class="banner_main" style="margin-top: -50px;">
    <h2 style="padding-top:50px;">Bạn có chắc muốn xóa video bài giảng này?</h2>

    <div class="form-horizontal">
        <hr />

        <div class="form-group">
            <strong>Tên video bài giảng: </strong>
            <span>@Model.NameVideo</span>
        </div>

        <div class="form-group">
            <strong>Link video: </strong>
            <span>@Model.LinkVideo</span>
        </div>

        <div class="form-group">
            <strong>Ngày đăng: </strong>
            <span>@Model.DateUpload.ToString("dd/MM/yyyy HH:mm:ss")</span>
        </div>

        <div class="form-group">
            <strong>Môn học: </strong>
            <span>@Model.NameSubject</span>
        </div>
    </div>

    @using (Html.BeginForm("DeleteConfirmed", "Videos", new { videoId = Model.Id }))
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Xác nhận Xóa" class="btn" style="background-color: red; color: white;" />
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Danh sách video", "Index")
    </div>

</section>


